{
  "schema_version": 1,
  "tool_id": "stats.forecast.v1",
  "display_name": "Linear trend forecast",
  "description": "Produces a deterministic bounded linear-trend forecast from an ordered numeric series.",
  "availability": "plan_step", "evaluation_only": true,
  "runtime": { "kind": "python", "audience": "stats-worker", "entrypoint": "toolbox:forecast", "image_digest": null },
  "operations": ["analyze"],
  "source_policy": { "required": false, "read_only": true, "relations_parameter": null },
  "parameter_schema": { "type": "object", "properties": {
    "values": { "type": "array", "items": { "type": "number" }, "minItems": 2, "maxItems": 50000 },
    "horizon": { "type": "integer", "minimum": 1, "maximum": 1000 }
  }, "required": ["values", "horizon"], "additionalProperties": false },
  "output_schema": { "type": "object", "properties": {
    "method": { "type": "string", "enum": ["linear_trend"] }, "slope": { "type": "number" }, "intercept": { "type": "number" },
    "forecast": { "type": "array", "items": { "type": "number" }, "minItems": 1, "maxItems": 1000 }
  }, "required": ["method", "slope", "intercept", "forecast"], "additionalProperties": false },
  "resource_limits": { "max_seconds": 60, "max_rows": 1000, "max_bytes": 1000000, "max_memory_mb": 512 },
  "network_policy": "deny_all",
  "determinism": { "deterministic": true, "seed_parameter": null, "records_runtime_version": true },
  "verifier_profile": "stats.forecast.v1"
}
